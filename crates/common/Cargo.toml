[package]
name = "common"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"
doctest = false


[dependencies]
database = { workspace = true }

serde_json = "1.0.114"
serde = { version = "1.0.197", features = ["derive"] }
